const {chromium}=require('@playwright/test'),assert=require('node:assert/strict'),fs=require('node:fs');
(async()=>{
 const browser=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true,args:['--use-angle=swiftshader','--enable-unsafe-swiftshader']});
 const page=await browser.newPage({viewport:{width:1440,height:900}}),errors=[];page.on('pageerror',e=>errors.push(e.message));
 const state=()=>page.evaluate(()=>JSON.parse(localStorage.getItem('pirate-clashers-v1')));
 async function seed(kind){await page.evaluate(async kind=>{const M=await import('/src/model.js'),s=M.fresh();s.onboarded=true;s.settings.sound=false;
  if(kind){const b=M.startBattle(s);s.moves=[0,3,5];s.move=kind==='gulls'?5:3;b.charged=true;
   if(kind==='solo'){const crew=M.active(b.player);crew.slice(1).forEach(g=>g.hp=0);}
   if(kind==='destroy'){b.enemy.hullParts.forEach(p=>p.hp=0);b.enemy.hull=0;delete b.enemy.hullMask;b.enemy.sailParts.forEach((p,i)=>p.hp=i===0?1:0);b.enemy.sails=100/280;b.enemy.mastParts.forEach((p,i)=>p.hp=i===0?1:0);}
  }localStorage.setItem('pirate-clashers-v1',JSON.stringify(s));},kind);await page.reload();}
 await page.goto('http://127.0.0.1:4173');await seed();await page.locator('[data-action=start]').click();await page.waitForSelector('#sheet.match-intro',{timeout:7000});
 assert.equal(await page.locator('.match-team').count(),2);assert.equal(await page.locator('.match-crew .framed-portrait').count(),6);assert.equal(await page.locator('.match-teams').locator(':scope > *').count(),5);assert.equal(await page.locator('.match-crew small,.match-crew strong').count(),0);assert.equal(await page.locator('.match-battle-button img').getAttribute('src'),'/public/menu-art/battle__8f2f3bff.png');
 for(const [name,width,height]of [['desktop',1440,900],['phone-landscape',932,430],['phone-portrait',390,844]]){await page.setViewportSize({width,height});await page.waitForTimeout(150);await page.screenshot({path:`test-results/battle-polish-matchup-${name}.png`});assert.ok(await page.locator('#sheet').evaluate(n=>n.scrollHeight<=n.clientHeight+2),'matchup fits: '+name);assert.ok(await page.locator('.player-crew').evaluate(n=>n.getBoundingClientRect().right<document.querySelector('.match-team.player').getBoundingClientRect().left),'player crew stays outside: '+name);assert.ok(await page.locator('.enemy-crew').evaluate(n=>n.getBoundingClientRect().left>document.querySelector('.match-team.enemy').getBoundingClientRect().right),'rival crew stays outside: '+name);}
 await page.setViewportSize({width:1440,height:900});await page.waitForTimeout(1100);await page.screenshot({path:'test-results/battle-polish-intro.png'});await page.locator('[data-action=begin-battle]').click();
 await page.waitForTimeout(600);assert.equal(await page.locator('[data-preview]').count(),0);assert.equal(await page.locator('#combatField').getAttribute('data-view'),'crew');assert.ok(await page.locator('[data-action=fire]').isDisabled());assert.equal(await page.locator('#phaseLabel').innerText(),'Choose Your Gunner');assert.ok(await page.locator('#weaponInfo').isHidden());
 assert.equal(await page.locator('#combatField > .battle-bottom').count(),1);assert.ok(await page.locator('#combatField #guns .framed-portrait').count()>0);assert.equal(await page.locator('#combatField .weapon-panel').evaluate(n=>getComputedStyle(n).position),'relative');
 const wheelBox=await page.locator('#shipWheel').boundingBox(),fieldBox=await page.locator('#combatField').boundingBox(),wheelVisible=fieldBox.y+fieldBox.height-wheelBox.y;assert.ok(wheelVisible>wheelBox.height*.28&&wheelVisible<wheelBox.height*.38,'top third of wheel visible at battle window bottom');
 await page.locator('[data-action=sail][data-id="1"]').click();await page.locator('[data-action=sail][data-id="-1"]').click();assert.equal((await state()).battle.movesLeft,0);
 assert.equal(await page.locator('[data-action=sail]').evaluateAll(nodes=>nodes.every(n=>n.hidden)),true);
 await page.locator('[data-action=scope]').click();await page.waitForTimeout(550);assert.equal(await page.locator('#combatField').getAttribute('data-view'),'scope');assert.equal(await page.locator('#combatField').getAttribute('data-scope'),'on');await page.screenshot({path:'test-results/battle-polish-overview.png'});
 await page.locator('#guns [data-action=gun]').first().click();await page.waitForTimeout(550);const selectedGunner=await page.locator('#guns [data-action=gun].active').getAttribute('data-id'),alternate=page.locator('#guns [data-action=gun]').last(),alternateId=await alternate.getAttribute('data-id');if(alternateId!==selectedGunner){await alternate.click();assert.equal(await page.locator('#combatField').getAttribute('data-selected'),alternateId);await page.locator('#guns [data-action=gun][data-id="'+selectedGunner+'"]').click();}assert.equal(await page.locator('#combatField').getAttribute('data-selected'),selectedGunner);
 assert.match(await page.locator('#weaponInfo').innerText(),/Lv\./);let infoBox=await page.locator('#weaponInfo').boundingBox(),crewBox=await page.locator('#guns').locator('xpath=..').boundingBox();assert.ok(Math.abs(infoBox.x-crewBox.x)<2&&crewBox.y+crewBox.height<=infoBox.y+1,'crew controls sit directly above gunner info');await page.screenshot({path:'test-results/battle-polish-gunner-window.png'});
 const playerFrame=()=>page.frames().find(f=>f.url().endsWith('ship.html?side=player'));
 assert.equal(await playerFrame().locator('[data-pedestal]').count(),1);assert.equal(await page.locator('#aimArc circle').count(),9);
 const field=await page.locator('#combatField').boundingBox(),x=field.width*.7,y=field.y+field.height*.35;
 const beforeCancel=(await state()).battle.shots;await page.mouse.move(x,y);await page.mouse.down();await page.mouse.move(x-70,y+35,{steps:5});await page.mouse.up();
 assert.equal(await page.locator('#combatField').getAttribute('data-selected'),selectedGunner);assert.equal((await state()).battle.shots,beforeCancel);
 await page.locator('#guns [data-action=gun]').first().click();await page.waitForTimeout(550);
 await page.mouse.move(x,y);await page.mouse.down();await page.mouse.move(x+90,y+48,{steps:7});await page.mouse.up();
 await page.waitForFunction(()=>JSON.parse(localStorage.getItem('pirate-clashers-v1')).battle.phase==='flight');
 assert.ok(await page.locator('[data-action=fire]').isDisabled());assert.ok(await page.locator('#flyingShots circle').count()>0);
 await page.screenshot({path:'test-results/battle-polish-flight.png'});
 await page.waitForFunction(()=>JSON.parse(localStorage.getItem('pirate-clashers-v1')).battle.phase==='player'&&!document.querySelector('#guns button').disabled,null,{timeout:15000});
 assert.equal((await state()).battle.events.length,1);assert.equal(await page.locator('#combatField').getAttribute('data-selected'),selectedGunner);
 await page.locator('#guns [data-action=gun]').first().click();await page.locator('#aimAngle').fill('25');await page.locator('[data-action=fire]').click();await page.waitForFunction(()=>document.querySelector('#phaseLabel').textContent==='Incoming Fire',null,{timeout:4000});
 await page.waitForFunction(()=>{const b=JSON.parse(localStorage.getItem('pirate-clashers-v1')).battle;return b.phase==='player'&&b.turn===2&&!document.querySelector('#guns button').disabled;},null,{timeout:30000});
 assert.equal((await state()).battle.events.length,4);
 await page.locator('#guns [data-action=gun]').first().click();await page.locator('#aimAngle').fill('75');await page.waitForTimeout(500);
 assert.ok(await page.locator('#aimArc circle').evaluateAll(nodes=>nodes.every(n=>Number(n.getAttribute('cy'))>=0)));
 await page.locator('#aimAngle').fill('35');await page.locator('[data-action=fire]').click();await page.waitForTimeout(150);await page.reload();await page.locator('[data-action=start]').click();
 await page.waitForFunction(()=>JSON.parse(localStorage.getItem('pirate-clashers-v1')).battle.phase==='player'&&!document.querySelector('#guns button').disabled,null,{timeout:15000});assert.equal((await state()).battle.events.length,5);await page.locator('#guns [data-action=gun]').first().click();await page.waitForTimeout(550);
 // Inspect layout without the turn expiring during software-rendered screenshots.
 await page.evaluate(()=>Object.defineProperty(document,'hidden',{configurable:true,get:()=>true}));
 for(const [name,width,height] of [['desktop',1440,900],['phone-landscape',932,430],['small-landscape',667,375],['phone-portrait',390,844]]){
 await page.setViewportSize({width,height});await page.waitForTimeout(650);
  assert.ok(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth&&document.documentElement.scrollHeight<=innerHeight),'no page scrolling: '+name);
  const info=await page.locator('#weaponInfo').boundingBox(),crew=await page.locator('#guns').locator('xpath=..').boundingBox();assert.ok(Math.abs(info.x-crew.x)<2&&crew.y+crew.height<=info.y+1,'crew controls above gunner info: '+name);
  for(const control of await page.locator('[data-action=fire],[data-action=finisher],[data-action=retreat],[data-action=sail],[data-action=scope]').all()){const r=await control.boundingBox();assert.ok(r&&r.x>=0&&r.y>=0&&r.x+r.width<=width+1&&r.y+r.height<=height+1,name+' '+await control.getAttribute('data-action')+' '+JSON.stringify(r));}
  await page.screenshot({path:'test-results/battle-polish-'+name+'.png'});
 }
 await page.setViewportSize({width:932,height:430});await seed('gulls');await page.locator('[data-action=start]').click();await page.waitForTimeout(500);await page.locator('[data-action=finisher]').click();
 await page.waitForFunction(()=>document.querySelector('#crewCheer').textContent.includes('GULLS'));assert.equal(await playerFrame().locator('body').getAttribute('data-reaction'),'laugh');
 await page.waitForTimeout(1300);
 await seed('destroy');await page.locator('[data-action=start]').click();await page.waitForTimeout(500);
 await page.evaluate(()=>new MutationObserver(()=>{if(!document.querySelector('#powderBlast').hidden&&!window.blastStarted)window.blastStarted=performance.now();}).observe(document.querySelector('#powderBlast'),{attributes:true}));await page.locator('[data-action=finisher]').click();
 await page.waitForSelector('#powderBlast:not([hidden])',{timeout:7000});const explosionAt=Date.now();await page.screenshot({path:'test-results/battle-polish-explosion.png'});
 await page.waitForTimeout(Math.max(0,3800-(Date.now()-explosionAt)));assert.ok(await page.locator('#powderBlast').isVisible());assert.equal(await page.locator('#sheet[open]').count(),0);
 await page.waitForFunction(()=>document.querySelector('#crewCheer').textContent.includes('VICTORY'),null,{timeout:7000});assert.ok(await page.evaluate(()=>performance.now()-window.blastStarted>=4900));
 assert.equal(await playerFrame().locator('body').getAttribute('data-reaction'),'victory');await page.screenshot({path:'test-results/battle-polish-celebration.png'});
 await page.waitForSelector('#sheet[open] .battle-result',{timeout:5000});assert.equal(await page.locator('#sheet[open] .result-port-button').count(),1);const result=(await state()).lastResult;assert.equal(result.won,true);assert.equal(result.lootBonus,0);
 await seed('solo');await page.locator('[data-action=start]').click();await page.waitForTimeout(500);assert.equal(await page.locator('#guns [data-action=gun].active').count(),1);assert.ok(await page.locator('[data-action=fire]').isEnabled());assert.equal(await page.locator('#combatField').getAttribute('data-selected'),String((await state()).battle.player.crew.find(g=>g.hp>0).id));assert.equal(await page.locator('#phaseLabel').innerText(),'Choose Your Gunner');assert.doesNotMatch(await page.locator('#weaponInfo').innerText(),/All hands, ready/);
 const steerStart=(await state()).battle,steerField=await page.locator('#combatField').boundingBox(),steerWheel=await page.locator('#shipWheel').boundingBox();await page.mouse.move(steerWheel.x+steerWheel.width/2,steerField.y+steerField.height-20);await page.mouse.down();await page.mouse.move(steerWheel.x+steerWheel.width/2+30,steerField.y+steerField.height-20,{steps:5});await page.mouse.up();const steered=(await state()).battle;assert.ok(steered.player.x>steerStart.player.x);assert.equal(steered.movesLeft,steerStart.movesLeft-1);
 assert.deepEqual(errors,[]);fs.writeFileSync('test-results/battle-polish-report.json',JSON.stringify({passed:true,errors,checks:['match introduction fits desktop and phone without scrolling','captain names on their own line with ships below','portrait-only crew icons on outside edges','correct Battle button art','preview cleanup','choose your gunner turn message','empty middle panel hidden when no gunner is selected','single surviving gunner auto-selected for attack','gold pedestal','water movement','telescope','short dot arc','drag cancellation','drag release fire','enemy turn','landscape and portrait viewport controls','seagull reaction','powder explosion','victory celebration','destruction reward']},null,2));
 console.log('Battle polish browser checks passed.');await browser.close();
})().catch(e=>{console.error(e);process.exit(1);});
