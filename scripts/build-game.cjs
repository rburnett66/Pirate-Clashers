const fs=require('node:fs'),path=require('node:path'),vm=require('node:vm');const root=path.resolve(__dirname,'..');if(path.basename(root)!=='1002 Pirate Crashers')throw Error('Wrong root');const read=n=>fs.readFileSync(path.join(root,'Code Prototypes',n),'utf8');const out=(n,s)=>fs.writeFileSync(path.join(root,n),s);fs.mkdirSync(path.join(root,'public'),{recursive:true});const crew=read('pirate-clashers-crew.html');const roster=vm.runInNewContext(crew.match(/const ROSTER = (\[[\s\S]*?\n  \]);/)[1]);const load=n=>{const box={module:{exports:{}},console};vm.runInNewContext(read(n),box);return box.module.exports;};const E=load('econ.js');const S=load('store.js');out('src/catalog.js','// Extracted from supplied prototypes; regenerate with npm run build.\nexport const ROSTER='+JSON.stringify(roster)+';\nexport const ECON='+JSON.stringify({...E,PACKS:S.PACKS,BUNDLES:S.BUNDLES})+';\n');let hull=read('pirate-clashers-hull-damage.html');const api=fs.readFileSync(path.join(root,'scripts/hull-adapter.txt'),'utf8');const end=hull.lastIndexOf('})();');hull=hull.slice(0,end)+api+'\n'+hull.slice(end);hull=hull.replace('</head>','<style>html,body{background:transparent!important}body>*:not(canvas):not(script):not(style):not(#scopeLabels):not(#pirateMark){display:none!important}#ship{position:fixed!important;inset:0!important;width:100%!important;height:100%!important}canvas{touch-action:none}</style></head>');hull=hull.replace("fire((px - canvas.width / 2) / cam.ppu + cam.x, (py - canvas.height / 2) / cam.ppu + cam.y);","parent.postMessage({type:'pirate-aim',side:new URLSearchParams(location.search).get('side'),x:(px-canvas.width/2)/cam.ppu+cam.x,y:(py-canvas.height/2)/cam.ppu+cam.y},location.origin);");hull=hull.replace('html,body{background:transparent!important}','html,body{color-scheme:dark;background:transparent!important}');hull=hull.replace('canvas.height / 2.6','canvas.height / 3.3').replace('cam.unit = 1 / cam.ppu;','cam.y = .85; cam.unit = 1 / cam.ppu;');hull=hull.replaceAll('GUNNER_TYPES[st.type].name','(st.name||GUNNER_TYPES[st.type].name)');out('public/ship.html',hull);let water=read('pirate-clashers-wheel-water-shader-only.html');water=water.replace('</head>','<style>body>*:not(canvas):not(script):not(style){display:none!important}canvas{pointer-events:none}</style></head>');water=water.replace('  resize();\n  resetSea();','  autoCannons=false; P.hullOn=false;\n  resize();\n  resetSea();');water=water.replace('hullOn: true','hullOn: false').replace('let autoCannons = !reduceMotion','let autoCannons = false');out('public/water.html',water);console.log('Reused 36 roster rows, economy/store tables and original water/hull renderers.');